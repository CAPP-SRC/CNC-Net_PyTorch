name: cnctorch-tpu
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - scipy
  - h5py
  - tqdm
  - pip
  - pip:
      # - torch>=2.1
      # - torch_xla>=2.1
      - open3d
      - pysdf
      - trimesh
      - PyMCubes
      - plyfile
      - -e ./required/pymesh2-0.3.1-cp310-cp310-linux_x86_64.whl
